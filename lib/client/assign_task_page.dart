import 'package:flutter/material.dart';

class AssignTaskPage extends StatelessWidget {
  const AssignTaskPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Assign Task Page"));
  }
}
